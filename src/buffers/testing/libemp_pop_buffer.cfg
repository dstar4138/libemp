{buffer_module, libemp_pop_buffer}.

% If 'all' is selected here, the internal max is 1K.
{buffer_take_count, 50}.

% This can be overridden via the user configs.
{buffer_known_events, [
    eventa,
    {eventb, "hello"},
    {eventb, "bye"},
    {eventc, "hello", "world"},
    {eventd, 100, 2.0}
]}.
